describe("quiz_service | utils | reportService", () => {
  test("placeholder: el suite carga y Jest no falla por archivo vacío", () => {
    expect(true).toBe(true);
  });

  test.todo("Agregar pruebas reales para buildStudentCourseReportData");
});
